version: 2.1

jobs:
  cb:
    docker:
      - image: circleci/python:latest

    steps: 
    - checkout
    - run: echo "Hi Chris"
    - run: echo $CIRCLE_BUILD_NUM

  bb:
    docker:
      - image: circleci/python:latest

    steps:
    - checkout
    - run: echo "Hi Benish"
    - run: echo $CIRCLE_BUILD_NUM

  anna:
    docker:
      - image: circleci/python:latest

    steps:
    - checkout
    - run: echo "Hi Anna"
    - run: echo $CIRCLE_BUILD_NUM



workflows:
  version: 2.1
  build_and_test:
    jobs:
      - cb
      - bb:
          requires:
            - cb
      - anna:
          requires:
            - bb

