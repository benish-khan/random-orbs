version: 2.1
jobs:
  build:
    docker:
      - image: cimg/ruby:2.7.1-node
    environment:
      FOO: Bebe
    steps:
      # - run: call api -> get the reponse -> FOO = Ben
      # - run: |
      # call api -> get the response -> FOO = Ben
    - run: echo export FOO=$FOO >> $BASH_ENV