version: 2.1

jobs:
  cb:
    docker:
      - image: circleci/python:latest
    working_directory: /tmp
    steps: 
      - run: echo "Hi Chris"
      - run: echo $CIRCLE_BUILD_NUM
      - run: mkdir -p workspace 
      - run: echo "Information from cb." > workspace/test
      - persist_to_workspace:
          root: workspace
          paths:
            - test

  bb:
    docker:
      - image: circleci/python:latest

    steps:
      - checkout
      - run: echo "Hi Benish"
      - run: echo $CIRCLE_BUILD_NUM
      - run: echo test_file_1
      - attach_workspace:
          at: /tmp/workspace

  anna:
    docker:
      - image: circleci/python:latest

    steps:
      - checkout
      - run: echo "Hi Anna"
      - run: echo $CIRCLE_BUILD_NUM



workflows:
  version: 2.1
  build_and_test:
    jobs:
      - cb
      - bb:
          requires:
            - cb
      - anna:
          requires:
            - bb


