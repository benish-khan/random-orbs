version: 2.1

jobs:
  cb:
    docker:
      - image: circleci/python:latest

    steps: 
    - checkout
    - run: echo "Hi Chris"
    - run: echo $CIRCLE_BUILD_NUM
    - run: touch test_file_1 echo "persisting this info from Hi Chris."
    - persist_to_workspace:
        root: ~/
        paths: /Git/circleci_sandbox

  bb:
    docker:
      - image: circleci/python:latest

    steps:
    - checkout
    - run: echo "Hi Benish"
    - run: echo $CIRCLE_BUILD_NUM
    - run: echo test_file_1
    - attach_workspace:
        at: /Git/circleci_sandbox

  anna:
    docker:
      - image: circleci/python:latest

    steps:
    - checkout
    - run: echo "Hi Anna"
    - run: echo $CIRCLE_BUILD_NUM



workflows:
  version: 2.1
  build_and_test:
    jobs:
      - cb
      - bb:
          requires:
            - cb
      - anna:
          requires:
            - bb


