version: 2.1

jobs:
  cb:
    docker:
      - image: circleci/python:latest

    steps: 
    - checkout
    - run: echo "Hi Chris"

  bb:
    docker:
      - image: circleci/python:latest

    steps:
    - checkout
    - run: echo "Hi Benish"


workflows:
  version: 2.1
  build_and_test:
    jobs:
      - cb
      - bb